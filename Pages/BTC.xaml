<Page x:Class="UserInterface.Pages.BTC"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:UserInterface.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="BTC" Height="720" Width="1280"
      TextElement.Foreground="{DynamicResource MaterialDesignBody}"
      Background="{x:Null}">
    
    <materialDesign:Card UniformCornerRadius="15" Background="{DynamicResource MaterialDesignPaper}" Margin="25" materialDesign:ShadowAssist.ShadowDepth="Depth4">
        <Grid>
            <TextBlock Text="BTC page"></TextBlock>
            <Button Click="backHomePage" Width="200">Back to homepage</Button>
        </Grid>
    </materialDesign:Card>
    
</Page>
